Things TODO:
- Debug why iter(dataset) in features.train is now a problem?
- Write a training file
- Reorganize directories per environment per agent/features
- Train on the server
- Streaming frames in streaming.py

- Divide models and logs per env
- collect training samples during reinforcement learning
- Batch, instance normalization?
- improve initizazion

Things done:
- Simple autoencoder
- Focus on a part of the image
- Tensorforce for dqn
